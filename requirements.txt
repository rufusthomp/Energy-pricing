# Installs the project and its pinned dependencies.
#
# The dependency list itself lives in pyproject.toml so there is one source of truth.
# Editable install so `gbmo` imports without a PYTHONPATH dance.
#
#   pip install -r requirements.txt          runtime
#   pip install -e ".[dev]"                  runtime + pytest

-e .
